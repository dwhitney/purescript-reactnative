{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "test": "pulp build -I test",
    "build": "pulp build"
  },
  "devDependencies": {
    "bower": "^1.8.2",
    "pulp": "^12.0.1",
    "rimraf": "^2.4.1"
  }
}
